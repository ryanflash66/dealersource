// Replace only with thin bindings to reviewed application code. No fixture answers here.
export const implementation = Object.freeze({
  kind: 'missing',
  revision: null,
  description: 'No application exists at the pinned repository baseline.'
});

export const operations = Object.freeze({});
