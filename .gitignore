# Parent repo is docs + scaffolding only. Keep it lean.
.env
.env.*
*.log
.DS_Store
Thumbs.db

# Eval run scratch output (commit curated summaries under results/ instead)
results/raw/

# Blind evaluator fixture sets. Never commit; agents can read this repo.
evals/fixtures/.private/
